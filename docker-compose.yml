services:
  postgres:
    image: postgres:16-alpine
    container_name: refund-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: refund_db
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  gradio:
    build: .
    container_name: refund-gradio
    ports:
      - "7861:7861"
    env_file:
      - .env
    environment:
      POSTGRES_HOST: postgres
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - index-data:/app/storage
      - ./data/bilgiler:/app/data/bilgiler
    command: python -m app.main
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "python -c 'import urllib.request; urllib.request.urlopen(\"http://localhost:7861\")'"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  django-api:
    build: .
    container_name: refund-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      POSTGRES_HOST: postgres
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - index-data:/app/storage
      - ./data/bilgiler:/app/data/bilgiler
      - django-db:/app/api
    command: python api/manage.py runserver 0.0.0.0:8000
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "python -c 'import urllib.request; urllib.request.urlopen(\"http://localhost:8000/api/v1/health/\")'"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

volumes:
  index-data:
  django-db:
  postgres-data:
